# 🎯 선생님 권한 시스템 테스트 가이드

## ⚠️ 중요: 반드시 먼저 해야 할 것

### 1단계: 로그아웃 및 캐시 클리어
```javascript
// 브라우저 개발자 도구 콘솔에서 실행
localStorage.clear()
location.reload()
```

**왜 필요한가?**
- 기존 localStorage에는 `permissions` 필드가 없음
- 새로 로그인해야 권한 정보가 포함됨

---

## 🔧 테스트 절차

### 1. 원장 계정으로 로그인

**URL**: https://superplace-academy.pages.dev/login

**계정 정보**:
- 이메일: `director@test.com`
- 비밀번호: `test1234!`

---

### 2. 선생님 생성 (아직 없는 경우)

1. **학생 관리 페이지로 이동**
   - URL: https://superplace-academy.pages.dev/students

2. **선생님 관리 섹션 열기**
   - "선생님 관리" 카드 클릭

3. **선생님 추가**
   - "선생님 추가" 버튼 클릭
   - 이름: 테스트선생님
   - 이메일: teacher@test.com
   - 비밀번호: test1234!
   - 전화번호: 010-1234-5678
   - 저장

---

### 3. 선생님 권한 설정

1. **권한 설정 버튼 클릭**
   - 방금 생성한 선생님의 "권한 설정" 버튼

2. **권한 설정**:
   - ❌ **전체 학생 조회 권한**: 체크 해제
   - ✅ **일일 성과 작성 권한**: 체크
   - ✅ **담당 반**: 
     - "초등 영어 초등 1학년 (초1)" 체크
     - 또는 원하는 반 1개 선택

3. **저장**

---

### 4. 로그아웃 및 선생님으로 로그인

1. **로그아웃**
   ```javascript
   // 개발자 도구 콘솔
   localStorage.clear()
   location.reload()
   ```

2. **선생님으로 로그인**
   - 이메일: `teacher@test.com`
   - 비밀번호: `test1234!`

3. **localStorage 확인**
   ```javascript
   const user = JSON.parse(localStorage.getItem('user'))
   console.log('User:', user)
   console.log('Permissions:', user.permissions)
   ```
   
   **예상 결과**:
   ```json
   {
     "id": 2,
     "email": "teacher@test.com",
     "user_type": "teacher",
     "parent_user_id": 1,
     "permissions": {
       "canViewAllStudents": false,
       "canWriteDailyReports": true,
       "assignedClasses": [5]
     }
   }
   ```

---

### 5. 학생 관리 페이지 확인

**URL**: https://superplace-academy.pages.dev/students

**확인 사항**:
- ❌ 선생님 관리 카드: **보이지 않아야 함**
- ❌ 반 관리 카드: **보이지 않아야 함**
- ✅ 학생 목록 카드: **표시되어야 함**
- ❌ 과목 관리 카드: **보이지 않아야 함**

---

### 6. 학생 목록 확인

**URL**: https://superplace-academy.pages.dev/students/list

**확인 사항**:
- ✅ **배정받은 반(5번)의 학생만 표시**
- ❌ **다른 반 학생은 보이지 않아야 함**

**개발자 도구 Network 탭**:
1. F12 → Network 탭
2. `/api/students` 요청 찾기
3. Request Headers 확인:
   ```
   X-User-Data-Base64: eyJ...
   ```
4. Response 확인:
   ```json
   {
     "success": true,
     "students": [/* 5번 반 학생만 */]
   }
   ```

---

### 7. 일일 성과 확인

**URL**: https://superplace-academy.pages.dev/students/daily-record

**확인 사항**:
- ✅ **학생 선택 드롭다운**: 5번 반 학생만 표시
- ❌ **다른 학생은 선택 불가**

---

## 🐛 문제 해결

### Q1: 여전히 모든 학생이 보입니다
**A**: 
1. localStorage를 확인하세요:
   ```javascript
   console.log(JSON.parse(localStorage.getItem('user')))
   ```
2. `permissions` 필드가 있는지 확인
3. 없다면 로그아웃 후 재로그인

### Q2: 권한 설정이 저장되지 않습니다
**A**: 
1. 원장 계정으로 로그인했는지 확인
2. 반이 생성되어 있는지 확인
3. 네트워크 탭에서 API 응답 확인

### Q3: 선생님 계정을 만들 수 없습니다
**A**:
1. 원장 계정인지 확인
2. 인증 코드를 사용한 가입인지 확인
3. 직접 선생님을 추가하는 기능 사용

---

## ✅ 성공 기준

다음 조건을 모두 만족하면 성공:

1. ✅ 로그인 응답에 `user_type`, `permissions` 포함
2. ✅ localStorage에 권한 정보 저장됨
3. ✅ 선생님 대시보드에서 제한된 카드만 표시
4. ✅ 학생 목록에서 배정받은 반 학생만 표시
5. ✅ 일일 성과에서 배정받은 학생만 선택 가능
6. ✅ API 요청에 `X-User-Data-Base64` 헤더 포함
7. ✅ API 응답이 필터링된 데이터만 반환

---

## 📊 API 테스트 (선택사항)

### curl로 직접 테스트

```bash
# 1. 로그인
curl -X POST 'https://superplace-academy.pages.dev/api/login' \
  -H 'Content-Type: application/json' \
  -d '{"email":"teacher@test.com","password":"test1234!"}'

# 2. 학생 조회 (선생님 권한)
curl 'https://superplace-academy.pages.dev/api/students' \
  -H 'X-User-Data-Base64: eyJpZCI6MiwidXNlcl90eXBlIjoidGVhY2hlciIsInBlcm1pc3Npb25zIjp7ImNhblZpZXdBbGxTdHVkZW50cyI6ZmFsc2UsImFzc2lnbmVkQ2xhc3NlcyI6WzVdfX0='

# 3. 학생 조회 (원장 권한)
curl 'https://superplace-academy.pages.dev/api/students?academyId=1'
```

---

**작성일**: 2026-01-18 00:45 KST
**버전**: 1.0
**커밋**: f8209e4
